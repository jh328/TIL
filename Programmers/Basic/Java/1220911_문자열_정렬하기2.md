```
class Solution{
    public String solution(String my_string){
        // 원본 참조 값 유지 하기 위해 반환값 새로운 변수에 할당
        String lowStr = my_string.toLowerCase();

        char[] charArray = lowStr.toCharArray();

        Arrays.sort(charArray);

        return new String(charArray);
    }
}
```