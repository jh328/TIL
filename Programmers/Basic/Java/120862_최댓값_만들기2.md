```
class Solution{
    public int solution(int[] numbers){
        int max = Integer.MIN_VALUE;

        for(int i = 0; i <numberes.length; i++){
            for(int j = i + 1; j <numbers.length; j++){
                
                int currentNum = numbers[i] * numbers[j];

                if(currentNum > max){
                    max = currentNum;
                }
            }
        }

        return max;
    }
}

이중 반복문 O(2ⁿ) 발생
```

```
class Solution {
    public int solution(int[] numbers){
        Arrays.sort(numbers);

        int len = numbers.length;

        int max = number[len - 1] * number[len - 2];

        int min = numbers[0] * numbers[1];

        return Math.max(max,min);
    }
}
```
